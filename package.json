{
  "name": "passforge",
  "private": true,
  "version": "0.1.0",
  "description": "PassForge - secure, local and multilingual password generator.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/generate-seo-pages.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "lucide": "^0.468.0"
  }
}
